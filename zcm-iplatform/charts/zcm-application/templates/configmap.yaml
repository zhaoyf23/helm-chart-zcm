apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zcm-application.fullname" . }}
  namespace: {{ .Values.global.namespace }}
data:
  {{- (.Files.Glob "files/*").AsConfig | nindent 2 }}