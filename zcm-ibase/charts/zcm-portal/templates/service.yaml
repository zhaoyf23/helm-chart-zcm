# Headless service for zcm-portal.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcm-portal.fullname" . }}
  labels:
    {{- include "zcm-portal.labels" . | nindent 4 }}
  namespace: {{ .Values.global.namespace }}
spec:
  clusterIP: {{ .Values.clusterIP }}
  type: {{ .Values.service.type }}
  ports:
    {{- toYaml .Values.service.ports | nindent 4 }}
  selector:
    {{- include "zcm-portal.selectorLabels" . | nindent 4 }}
