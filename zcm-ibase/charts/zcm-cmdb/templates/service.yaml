# Headless service for zcm-cmdb.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcm-cmdb.fullname" . }}
  labels:
    {{- include "zcm-cmdb.labels" . | nindent 4 }}
  namespace: {{ .Values.global.namespace }}
spec:
  clusterIP: {{ .Values.clusterIP }}
  type: {{ .Values.service.type }}
  ports:
    {{- toYaml .Values.service.ports | nindent 4 }}
  selector:
    {{- include "zcm-cmdb.selectorLabels" . | nindent 4 }}
