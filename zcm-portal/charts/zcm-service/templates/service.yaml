# Headless service for zcm-service.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcm-service.fullname" . }}
  labels:
    {{- include "zcm-service.labels" . | nindent 4 }}
  namespace: {{ .Values.global.namespace }}
spec:
  clusterIP: {{ .Values.clusterIP }}
  type: {{ .Values.service.type }}
  ports:
    {{- toYaml .Values.service.ports | nindent 4 }}
  selector:
    {{- include "zcm-service.selectorLabels" . | nindent 4 }}
