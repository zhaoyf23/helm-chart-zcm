# Headless service for zcm-application.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcm-application.fullname" . }}
  labels:
    {{- include "zcm-application.labels" . | nindent 4 }}
  namespace: {{ .Values.global.namespace }}
spec:
  clusterIP: {{ .Values.clusterIP }}
  type: {{ .Values.service.type }}
  ports:
    {{- toYaml .Values.service.ports | nindent 4 }}
  selector:
    {{- include "zcm-application.selectorLabels" . | nindent 4 }}
