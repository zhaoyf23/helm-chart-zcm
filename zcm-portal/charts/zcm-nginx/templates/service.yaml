# Headless service for zcm-nginx.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcm-nginx.fullname" . }}
  labels:
    {{- include "zcm-nginx.labels" . | nindent 4 }}
  namespace: {{ .Values.global.namespace }}
spec:
  clusterIP: {{ .Values.clusterIP }}
  type: {{ .Values.service.type }}
  ports:
    {{- toYaml .Values.service.ports | nindent 4 }}
  selector:
    {{- include "zcm-nginx.selectorLabels" . | nindent 4 }}
---
# Client service for zcm-nginx.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcm-nginx.fullname" . }}-outer
  labels:
    {{- include "zcm-nginx.labels" . | nindent 4 }}
  namespace: {{ .Values.global.namespace }}
spec:
  type: {{ .Values.service.typeouter }}
  ports:
    {{- toYaml .Values.service.portsouter | nindent 4 }}
  selector:
    {{- include "zcm-nginx.selectorLabels" . | nindent 4 }}