# Headless service for zcm-zcamp-rest-server.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcm-zcamp-rest-server.fullname" . }}
  labels:
    {{- include "zcm-zcamp-rest-server.labels" . | nindent 4 }}
  namespace: {{ .Values.global.namespace }}
spec:
  clusterIP: {{ .Values.clusterIP }}
  type: {{ .Values.service.type }}
  ports:
    {{- toYaml .Values.service.ports | nindent 4 }}
  selector:
    {{- include "zcm-zcamp-rest-server.selectorLabels" . | nindent 4 }}
